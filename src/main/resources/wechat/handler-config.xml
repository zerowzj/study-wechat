<?xml version="1.0" encoding="UTF-8"?>
<handlers xmlns="http://www.company.cn/handlers"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.company.cn/handlers /META-INF/handlers.xsd">
    <!-- ==================== -->
    <!--         事件         -->
    <!-- ==================== -->
    <!-- 关注/取消关注 -->
    <handler msgType="event" event="subscribe" bean="subscribeHandler"/>
    <handler msgType="event" event="unsubscribe" bean="unsubscribeHandler"/>
    <!-- 跳转链接 -->
    <!--<handler msgType="event" event="VIEW" bean="viewHandler"/>-->
    <!-- 拉取消息 -->
    <!--<handler msgType="event" event="CLICK" eventKey="click_1" bean="clickHandler"/>-->

    <!-- ==================== -->
    <!--         消息         -->
    <!-- ==================== -->
    <!-- 文本 -->
    <handler msgType="text" bean="textHandler"/>
    <!-- 图片 -->
    <!--<handler msgType="image" bean="imageHandler"/>-->
</handlers>